{
  "name": "tess",
  "scripts": {
    "dev:core": "cargo run --release --manifest-path tess-core/Cargo.toml",
    "dev:stt": "uv run tess-stt/main.py",
    "dev": "concurrently -n core,stt -c blue,green \"bun run dev:core\" \"bun run dev:stt\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "wait-on": "^9.1.0"
  }
}